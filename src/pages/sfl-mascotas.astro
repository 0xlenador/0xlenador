---
import Layout from "../layouts/Layout.astro"
import { getLangFromUrl, useTranslations } from "../i18n/utils"
import SflPetsView from "../components/views/SflPetsView.astro"

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)

const seo = {
  title: t("sfl.pets.seo.title"),
  description: t("sfl.pets.seo.description"),
  image: "/img/meta/sunflowerland-og.webp",
}
---

<Layout title={seo.title} description={seo.description} image={seo.image} customSpacing>
  <SflPetsView />
</Layout>
